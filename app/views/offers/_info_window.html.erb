
<div class="mapboxgl-popup-content">
  <h4 class="fw-bold"><%= offer.name %></h4>
  <p><%= offer.location %></p>
  <p><%= offer.unit_price.to_i %>  <%= offer.currency %></p>
  <div class="mapboxgl-popup-img">
  <%= cl_image_tag(offer.photo.key, width: 100, height: 100, quality: 60) %>
  </div>
</div>
