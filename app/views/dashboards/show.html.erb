<div class="container-fluid">

  <h2><%= "Welcome #{ @user.username }"%></h2>

 <h3><%= "Here are your upcoming bookings"%></h3>
  <%# <div class="container d-flex">
      <% @user.bookings.select { |booking| booking.upcoming%>
      <%# <div class="card">
        <%= cl_image_tag(booking.offer.photo.key, width: 100, height: 100, crop: :fill) %>
        <%# <%= booking.offer.name %>
        <%# <%= booking.date %>
      <%# </div>
      <%# <% } %>
  <%# </div> %>
    <div class="container d-flex">
      <% @user.bookings.select { |booking| booking.date >= Date.today %>
      <div class="card">
        <%= cl_image_tag(booking.offer.photo.key, width: 100, height: 100, crop: :fill) %>
        <%= booking.offer.name %>
        <%= booking.date %>
      </div>
      <% }%>
  </div>



  <h3><%= "Here are your past bookings"%></h3>
  <div class="container d-flex">
      <% @user.bookings.select { |booking| booking.date < Date.today %>
      <div class="card">
        <%= cl_image_tag(booking.offer.photo.key, width: 100, height: 100, crop: :fill) %>
        <%= booking.offer.name %>
        <%= booking.date %>
      </div>
      <% }%>
  </div>

</div>
